# 🧾 SmartShop Web API 規格書 (Draft v1)

**Base URL:**
`http://localhost:8080`

---

## 🛍️ ProductController

**Request Mapping:** `/products`

| Method     | Path      | 說明          | 範例 URL                         | 備註             |
| ---------- | --------- | ----------- | ------------------------------ | -------------- |
| **GET**    | `""`, `/` | 查詢所有商品 (多筆) | `/products` or `/products/`    | 回傳商品列表         |
| **GET**    | `/{id}`   | 查詢指定商品 (單筆) | `/products/1` or `/products/2` | 若查無資料，回傳 404   |
| **POST**   | `""`, `/` | 新增商品        | `/products` or `/products/`    | 僅管理員可用（未來可加權限） |
| **PUT**    | `/{id}`   | 修改指定商品      | `/products/1`                  | 未來提供編輯功能       |
| **DELETE** | `/{id}`   | 刪除商品        | `/products/1`                  | 未來提供刪除功能       |

---

## 🔐 AuthController

**Request Mapping:** `/auth`

| Method   | Path          | 說明     | 範例 URL             | 備註                 |
| -------- | ------------- | ------ | ------------------ | ------------------ |
| **POST** | `/login`      | 登入     | `/auth/login`      | 傳入帳號密碼             |
| **GET**  | `/logout`     | 登出     | `/auth/logout`     | 清除 session 或 token |
| **GET**  | `/isLoggedIn` | 判斷登入狀態 | `/auth/isLoggedIn` | 回傳使用者是否已登入         |

---

## 📦 OrderController

**Request Mapping:** `/orders`

| Method   | Path        | 說明              | 範例 URL                  | 備註            |
| -------- | ----------- | --------------- | ----------------------- | ------------- |
| **GET**  | `""`, `/`   | 查詢該用戶 (已登入) 的訂單 | `/orders` or `/orders/` | 需登入後才能使用      |
| **POST** | `/checkout` | 該用戶 (已登入) 進行結帳  | `/orders/checkout`      | 建立新訂單並可搭配付款模組 |

---

## ❤️ FavoriteController

**Request Mapping:** `/favorites`

| Method     | Path                   | 說明               | 範例 URL                        | 備註       |
| ---------- | ---------------------- | ---------------- | ----------------------------- | -------- |
| **GET**    | `""`, `/`              | 獲取用戶 (已登入) 的關注清單 | `/favorites` or `/favorites/` | 顯示目前收藏商品 |
| **GET**    | `/product/{productId}` | 獲取該商品被哪些用戶關注     | `/favorites/product/1`        | 管理者可用    |
| **POST**   | `/{productId}`         | 用戶 (已登入) 加入關注商品  | `/favorites/1`                | 新增收藏     |
| **DELETE** | `/{productId}`         | 用戶 (已登入) 移除關注商品  | `/favorites/1`                | 移除收藏     |

---

## 📘 統一回應格式 (ApiResponse)

所有 API 回傳皆使用以下 JSON 格式：

```json
{
  "status": 200,
  "message": "操作成功",
  "data": { }
}
```

錯誤範例：

```json
{
  "status": 404,
  "message": "找不到商品 ID：99",
  "data": null
}
```

---

## 🧠 授權 (Authentication) 規劃（預留）

| 模組       | 是否需登入 | 備註         |
| -------- | ----- | ---------- |
| Product  | ❌ 否   | 瀏覽商品開放     |
| Auth     | ❌ 否   | 登入、登出、狀態判斷 |
| Order    | ✅ 是   | 需登入才能結帳    |
| Favorite | ✅ 是   | 收藏需登入      |

---

## 🧩 未來擴充模組（預定）

| 模組                | Request Mapping | 功能              |
| ----------------- | --------------- | --------------- |
| CartController    | `/cart`         | 加入購物車 / 查看購物車內容 |
| PaymentController | `/payment`      | 串接綠界模擬付款流程      |
| AiController      | `/ai`           | 智能推薦商品 / 智慧客服   |
